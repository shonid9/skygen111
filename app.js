const ro=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');ro.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>ro.observe(e));const btn=document.querySelector('.mobileBtn'),panel=document.querySelector('.mobilePanel');btn?.addEventListener('click',()=>panel.classList.toggle('open'));panel?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>panel.classList.remove('open')));const fine=matchMedia('(hover:hover) and (pointer:fine)').matches,probe=document.querySelector('.probe');if(fine&&probe){addEventListener('mousemove',e=>{probe.style.left=e.clientX+'px';probe.style.top=e.clientY+'px';probe.classList.add('on')});addEventListener('mouseleave',()=>probe.classList.remove('on'));document.querySelectorAll('a,.btn,.card,.chip,.row,.integration').forEach(el=>{el.addEventListener('mouseenter',()=>probe.classList.add('big'));el.addEventListener('mouseleave',()=>probe.classList.remove('big'))})}